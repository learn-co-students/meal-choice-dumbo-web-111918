# Define a method meal_choice that returns the meal_choice that was
# passed into it and defaults to meat.
def meal_choice(meal="meat") #set default value to "meat"
  meal #call "meal" variable to be the last line of code of it is what is returned
end
